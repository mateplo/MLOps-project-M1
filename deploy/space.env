# Runtime configuration injected into the Space's Dockerfile by the deploy-space CI job.
# MODEL_REVISION: git revision of the HF model repo to serve ("main" = latest published champion,
# or a tag such as "v9" to pin a registry version).
MODEL_REVISION=main
