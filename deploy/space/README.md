---
title: Adult Income Classifier
emoji: 💼
colorFrom: blue
colorTo: green
sdk: docker
app_port: 8000
pinned: false
license: mit
---

# Adult Income Classifier — API

FastAPI microservice deployed from the GitHub repository
[mateplo/MLOps-project-M1](https://github.com/mateplo/MLOps-project-M1).

- The image comes from GHCR (built and published by GitHub Actions on a `v*` tag).
- The model is downloaded at start-up from the Hugging Face model repo (the published champion).
- `GET /health` tells you exactly which registry version / run_id is being served.
- `POST /predict`, `GET /docs`, `GET /metrics` (Prometheus).

This Space is **generated by CI**: do not edit it by hand, open a pull request on GitHub instead.
